name:  Daily Portfolio Analytics

on:
  schedule:
    # Lundi-Vendredi à 17:00 UTC (18:00 CET / 19:00 CEST)
    - cron: '0 17 * * 1-5'
  
  workflow_dispatch:  # Bouton manuel

permissions:
  contents: write
  actions: read

jobs:
  portfolio-optimization:
    name: Run ML Pipeline & Update Portfolio
    runs-on: ubuntu-latest
    timeout-minutes: 15
    
    steps:
      # ══════════════════════════════════════════════════════
      # 1. Setup Environment
      # ══════════════════════════════════════════════════════
      - name: 📥 Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      
      # TRES BIEN D'AVOIR CHOISI 3.11 (Évite les bugs de 3.13 avec Pandas)
      - name: 🐍 Setup Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
      
      - name: 📦 Install dependencies
        run: |
          python -m pip install --upgrade pip setuptools wheel
          pip install -r requirements.txt
      
      
      # ══════════════════════════════════════════════════════
      # 2. Run Analytics Pipeline
      # ══════════════════════════════════════════════════════
      - name: Execute Portfolio Optimization
        run: |
          echo "🚀 Starting portfolio analytics pipeline..."
          python daily_run.py
          echo "✅ Pipeline completed successfully"
        env:
          PYTHONUNBUFFERED: 1
          TZ: Europe/Paris
      
      
      # ══════════════════════════════════════════════════════
      # 3. Save Results
      # ══════════════════════════════════════════════════════
      - name: 💾 Commit updated data
        run: |
          # Configure Git
          git config --local user.name 'AlphaML Bot'
          git config --local user.email '41898282+github-actions[bot]@users.noreply.github.com'
          
          # 1. On ajoute TOUS les fichiers (y compris le debug) en une seule fois
          # Note: Si debug_run.txt n'existe pas, git add ne plantera pas fatalement si les autres existent.
          git add latest_signals.csv portfolio_history.csv debug_run.txt
          
          # Commit if changes exist
          if ! git diff --staged --quiet; then
            TIMESTAMP=$(date +'%Y-%m-%d %H:%M UTC')
            git commit -m "chore(data): daily portfolio update ${TIMESTAMP}"
            
            # --- SECURITÉ CRUCIALE ---
            # On récupère les dernières modifs du site avant de pousser pour éviter le rejet
            git pull --rebase origin main
            
            git push
            echo "✅ Data committed and pushed"
          else
            echo "ℹ️ No changes to commit"
          fi
      
      # ══════════════════════════════════════════════════════
      # 4. Summary (Optional)
      # ══════════════════════════════════════════════════════
      - name:  Generate job summary
        if: always()
        run: |
          echo "## 📊 Portfolio Analytics Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- **Date**: $(date +'%Y-%m-%d %H:%M UTC')" >> $GITHUB_STEP_SUMMARY
          echo "- **Status**: ${{ job.status }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Run Number**: #${{ github.run_number }}" >> $GITHUB_STEP_SUMMARY
          
          if [ -f latest_signals.csv ]; then
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "### 📈 Latest Signals (Top 5)" >> $GITHUB_STEP_SUMMARY
            echo '```csv' >> $GITHUB_STEP_SUMMARY
            head -n 6 latest_signals.csv >> $GITHUB_STEP_SUMMARY
            echo '```' >> $GITHUB_STEP_SUMMARY
          fi