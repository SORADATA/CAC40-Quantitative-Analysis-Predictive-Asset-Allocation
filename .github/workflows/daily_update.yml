name: Daily Portfolio Analytics

on:
  schedule:
    # Lundi-Vendredi à 17:00 UTC (18:00 CET / 19:00 CEST)
    - cron: '0 17 * * 1-5'
  
  workflow_dispatch:  # Bouton manuel

permissions:
  contents: write
  actions: read

jobs:
  portfolio-optimization:
    name: Run ML Pipeline & Update Portfolio
    runs-on: ubuntu-latest
    timeout-minutes: 15
    
    steps:
      # ══════════════════════════════════════════════════════
      # 1. Setup Environment
      # ══════════════════════════════════════════════════════
      - name: 📥 Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      
      - name: 🐍 Setup Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
      
      - name: 📦 Install dependencies
        run: |
          python -m pip install --upgrade pip setuptools wheel
          pip install -r requirements.txt
      
      
      # ══════════════════════════════════════════════════════
      # 2. Run Analytics Pipeline
      # ══════════════════════════════════════════════════════
      - name: 🚀 Execute Portfolio Optimization
        run: |
          echo "🚀 Starting portfolio analytics pipeline..."
          python daily_run.py
          echo "✅ Pipeline completed successfully"
        env:
          PYTHONUNBUFFERED: 1
          TZ: Europe/Paris
      
      
      # ══════════════════════════════════════════════════════
      # 3. Verify Generated Files (Debugging only)
      # ══════════════════════════════════════════════════════
      - name: 🔍 Check generated files
        run: |
          echo "📂 Checking generated files..."
          ls -lh latest_signals.csv portfolio_history.csv data_metadata.json || echo "Some output files missing"
      
      
      # ══════════════════════════════════════════════════════
      # 4. Commit & Push Results (FIXED: NO PARQUET / FORCE MERGE)
      # ══════════════════════════════════════════════════════
      - name: 💾 Commit updated data
        run: |
          git config --local user.name 'AlphaML Bot'
          git config --local user.email '41898282+github-actions[bot]@users.noreply.github.com'
          
          # 1. Add ONLY result files (We intentionally IGNORE parquet files to avoid conflicts)
          git add latest_signals.csv portfolio_history.csv data_metadata.json debug_run.txt
          
          # 2. Check if there are changes to commit
          if ! git diff --staged --quiet; then
            TIMESTAMP=$(date +'%Y-%m-%d %H:%M UTC')
            git commit -m "chore(data): daily portfolio update ${TIMESTAMP}"
            
            # 3. CRUCIAL FIX: Pull with rebase strategy "Theirs"
            # This forces the bot's version to win if there is a conflict on the CSVs
            git pull --rebase -Xtheirs origin main
            
            # 4. Push
            git push origin main
            echo "✅ Data committed and pushed successfully"
          else
            echo "ℹ️ No changes to commit"
          fi
      
      
      # ══════════════════════════════════════════════════════
      # 5. Job Summary
      # ══════════════════════════════════════════════════════
      - name: 📊 Generate job summary
        if: always()
        run: |
          echo "## 📊 Portfolio Analytics Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- **Date**: $(date +'%Y-%m-%d %H:%M UTC')" >> $GITHUB_STEP_SUMMARY
          echo "- **Status**: ${{ job.status }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Run Number**: #${{ github.run_number }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          
          if [ -f latest_signals.csv ]; then
            echo "### 📈 Latest Signals (Top 5)" >> $GITHUB_STEP_SUMMARY
            echo '```csv' >> $GITHUB_STEP_SUMMARY
            head -n 6 latest_signals.csv >> $GITHUB_STEP_SUMMARY
            echo '```' >> $GITHUB_STEP_SUMMARY
          fi